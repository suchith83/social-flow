[pytest]
minversion = 7.0
addopts = -q --showlocals
testpaths =
    services
    app
    analytics
    live-streaming
    ml-pipelines
    mobile-backend
markers =
    integration: mark slow integration tests
    smoke: quick smoke tests
