# Base URL under test
BASE_URL=http://localhost:3000/api

# Auth credentials used by auth scenario for token acquisition
TEST_USER_USERNAME=admin
TEST_USER_PASSWORD=password123

# Storage test parameters (upload/download)
STORAGE_BUCKET=test-bucket
STORAGE_SAMPLE_FILE_PATH=./fixtures/sample.txt

# Artillery scenario tuning
VUS_DEFAULT=50
DURATION_DEFAULT=60      # seconds
RAMP_UP_SECONDS=30

# Optional proxy for outbound requests (if your environment requires it)
HTTP_PROXY=

# Reporting
REPORT_DIR=./reports
