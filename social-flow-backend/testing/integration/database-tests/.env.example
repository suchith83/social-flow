# Database connection examples used by tests. Copy to .env and modify as needed.
DB_VENDOR=postgres        # postgres | cockroach
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=testdb

# For CockroachDB example:
# DB_VENDOR=cockroach
# DB_PORT=26257
# DB_USER=root
# DB_PASSWORD=
# DB_NAME=defaultdb

# Other settings
TEST_SCHEMA_PREFIX=test_  # optional prefix to isolate test tables
