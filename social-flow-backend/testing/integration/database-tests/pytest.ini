[pytest]
minversion = 7.0
addopts = -ra -q --maxfail=1
testpaths = tests
python_files = test_*.py
markers =
    integration: mark integration tests (requires DB)
    slow: mark slow tests (performance)
