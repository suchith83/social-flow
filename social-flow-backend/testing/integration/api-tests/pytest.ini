[pytest]
minversion = 7.0
addopts = -ra -q --maxfail=1
testpaths = tests
python_files = test_*.py
markers =
    integration: mark integration tests
    contract: mark contract tests
    smoke: smoke tests
