"""
Custom scalars used across the schema.

Notes:
- DateTime: ISO 8601 UTC timestamps. Implement using a robust scalar library (e.g., graphql-scalars).
- JSON: Arbitrary JSON object. Use GraphQLJSON (graphql-type-json) or equivalent.
- URL, Email: validated scalar types can help offload simple validation.
- Cursor: opaque cursor string for Relay-style cursor pagination (e.g., base64 encoded "id:timestamp").
"""
scalar DateTime
scalar JSON
scalar URL
scalar Email
scalar Cursor
