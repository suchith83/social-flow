"""
Centralized enums so mutation & query files import consistent values.
Change/extend these as product features evolve.
"""

enum UserRole {
  USER
  CREATOR
  MODERATOR
  ADMIN
  SERVICE
}

enum PostVisibility {
  PUBLIC
  UNLISTED
  PRIVATE
  FOLLOWERS_ONLY
}

enum VideoVisibility {
  PUBLIC
  UNLISTED
  PRIVATE
  MEMBERS_ONLY
}

enum ReactionType {
  LIKE
  LOVE
  LAUGH
  SAD
  ANGRY
  WOW
}

enum MediaType {
  IMAGE
  VIDEO
  AUDIO
  FILE
  OTHER
}

enum PaymentStatus {
  PENDING
  PAID
  FAILED
  REFUNDED
}

enum BillingInterval {
  MONTHLY
  YEARLY
  WEEKLY
  ONE_TIME
}

enum SubscriptionStatus {
  ACTIVE
  PAST_DUE
  CANCELLED
  TRIALING
  EXPIRED
}

enum ReportSeverity {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum ReportStatus {
  OPEN
  IN_REVIEW
  RESOLVED
  DISMISSED
}

enum ContentType {
  POST
  VIDEO
  COMMENT
  USER
  MESSAGE
}

enum ModerationAction {
  REMOVED
  HIDDEN
  FLAGGED
  RESTORED
}

enum NotificationCategory {
  MENTIONS
  REACTIONS
  FOLLOWS
  REPLIES
  SYSTEM
  SUBSCRIPTIONS
  PAYMENTS
}

enum NotificationPriority {
  CRITICAL
  HIGH
  MEDIUM
  LOW
}
