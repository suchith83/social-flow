config:
  target: "${ARTILLERY_TARGET}"
  processor: "../scripts/generate_jwt.js"
  phases:
    - duration: 30
      arrivalRate: 1

scenarios:
  - name: "smoke"
    flow:
      - function: "setupAuth"
      - get:
          url: "/health"
      - get:
          url: "/v1/explore"
