apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: network-latency
  namespace: default
spec:
  action: delay
  mode: all
  selector:
    labelSelectors:
      "app": "my-service"
  delay:
    latency: "200ms"
    correlation: "50"
  duration: "60s"
